# The pipeline is standard-library only (urllib, json, sqlite3) — no third-party
# packages required. This file exists so the GitHub Actions `pip install -r` step
# is a no-op and stays consistent with the commodity workflow.
